## 2026-07-09 Northstar reset
- Changed: created Ralph run folder for the 72h commercial-product push.
- Verified: pending.
- Learned: the work is not a UI hotfix; the product must make DB/SIF/Vision/Ontology/Learning visibly change workpack output and sharing readiness.
- Next: N1 separate generated vs share-ready state with a failing test first.

## 2026-07-09 N1-N2
- Changed: added `lib/workpack-readiness.ts`, wired readiness into workspace topbar and share panel, strengthened DB harness reflection into risk assessment/TBM/photo evidence deliverables, moved edit flow toward textarea-first workspace surface.
- Verified: `npm.cmd test -- tests\workpack-readiness.test.ts tests\commercial-harness.test.ts tests\quality-contract.test.ts tests\operation-improvements.test.ts tests\photo-vision-analysis.test.ts`; `npm.cmd run typecheck`; `npm.cmd run build`; local browser probe for `/workspace`.
- Learned: SIF embedding corpus/approval gate is prepared, but Supabase vector table/RPC/upload are still approval-gated and not executed.
- Next: N3 SIF embedding migration approval packet, then N4 deterministic photo hazard rows and N5 workpack operation graph explorer.

## 2026-07-09 N4
- Changed: accepted photo/Vision/OCR harness memory now creates deterministic structured risk assessment rows and TBM risk links, with photo names, OCR, site signals, and vision evidence preserved in evidence refs.
- Verified: `npm.cmd test -- tests\workpack-readiness.test.ts tests\commercial-harness.test.ts tests\quality-contract.test.ts tests\operation-improvements.test.ts tests\photo-vision-analysis.test.ts tests\reporting-downloads.test.ts`; `npm.cmd run typecheck`.
- Learned: photo analysis was already reaching harness memory, but structured risk rows were still mostly AI/fallback-driven. N4 makes accepted image risk candidates consequential even when prose generation is template/fallback.
- Next: N3 SIF migration approval packet remains DB-approval gated; N5 workpack operation graph explorer is the next schema-free product surface.

## 2026-07-09 UI overlap regression
- Changed: added a Day-theme Playwright layout regression test and a final workspace overlap guard so topbar, side nav, main surface, headline, and textarea cannot visually cover each other.
- Verified: red `npm.cmd test -- tests\workspace-layout-regression.test.ts` failed on first-screen line-height; green rerun passed 3 tests after CSS guard and line-height fix.
- Learned: the workspace CSS has several historical visual passes, so final shell geometry must be guarded until the stylesheet is consolidated.
- Next: continue route-by-route design harmonization after the N4/N5 product proof work.

## 2026-07-09 wide-short Day scroll overlap
- Changed: widened the workspace topbar guard to all `.command-center-shell .command-topbar` nodes and added a Day + `2048x638` scrolled regression test.
- Verified: live production reproduced the overlap with sticky topbar over the headline; local HEAD scrolls the topbar away. `npm.cmd test -- tests\workspace-layout-regression.test.ts` passed 4 tests.
- Learned: the user screenshot matched a presentation-like wide/short viewport after scroll, not only the first-load desktop layout.
- Next: deploy/merge path must be resolved before using `www.safeclaw.kr/workspace` as the demo surface.

## 2026-07-09 N5 workspace operation ontology
- Changed: added `lib/workspace-operation-graph.ts`, extended operation memory with similar past workpack relationships, refactored the graph UI into `OperationMemoryGraphViewer`, and rendered the operation graph inside `/workspace` after the evidence panel.
- Verified: `npm.cmd test -- tests\workspace-operation-graph.test.ts tests\operation-memory-visualization.test.ts tests\workspace-layout-regression.test.ts`; browser desktop/mobile captures in `evaluation/northstar-72h-2026-07-09/ontology-browser-check`.
- Learned: the first graph was correct but too dense, so workspace graph nodes now stay compact until active/hovered and the mobile list uses bounded internal scroll.
- Next: N3 SIF embedding approval packet remains approval-gated; N6 route-by-route design harmonization remains open.
