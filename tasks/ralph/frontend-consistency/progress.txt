## 2026-07-11 F1
- Changed: added exact typography, spacing, radius, route, and generated-surface contracts plus a static audit command.
- Verified: 32 discovered routes, zero route coverage gaps, focused contract tests pass, typecheck passes.
- Learned: the original stylesheet contained 637 off-contract declarations before line-height and tracking were added to the audit.
- Next: F2 independent review and browser-safe correction of mechanical normalization.

## 2026-07-11 F2
- Changed: removed all important declarations and mechanically mapped font sizes, line heights, tracking, and radii to approved tiers; restored explicit circle and structural geometry.
- Verified: focused contract tests, static audit, and typecheck currently pass.
- Learned: static compliance alone cannot prove semantic role correctness after a bulk map.
- Next: independent diff review, targeted fixes, then browser evidence before marking passed.

## 2026-07-11 F2 review closure
- Changed: normalized 431 font-size rules to complete semantic tuples and enforced per-selector mixed-role, exact effect, circle, cascade, and reduced-motion contracts.
- Verified: focused contract test 18/18, static audit 32/32 with zero violations, typecheck, build 27/27, independent spec and code-quality review both pass with zero findings.
- Learned: class-name keywords cannot prove interactive or HUD semantics; ambiguous current selectors need explicit roles and mixed selector lists must be classified per selector.
- Next: F3 shared shells and framework states.

## 2026-07-11 F2 review remediation
- Changed: normalized later duplicate control and active-state rules so the canonical cascade wins; removed the table-header tracking tier and decorative effects; added selector-aware role/effect audit checks and complete reduced-motion coverage.
- Verified: focused contract tests pass (8/8), static audit passes for 32 routes with zero violations, typecheck passes, and production build passes.
- Learned: named rule-block assertions and adversarial audit fixtures are required to detect cascade and semantic-role false positives.
- Next: keep F2 `passes: false` until independent reviewer approval; then continue browser evidence in the later route tasks.

## 2026-07-11 F2 re-review remediation
- Changed: restored four true-circle roles, mapped cited scoped selectors to complete semantic tiers, covered more-specific document-module headings, and replaced effect substring allowlisting with exact normalized selector/value contracts.
- Verified: focused contract tests pass (11/11), static audit passes for 32 routes with zero violations, typecheck passes, and production build passes with 27/27 static pages generated.
- Learned: allowlisting a selector name is insufficient unless the exact property value is also contracted; scoped role checks must include the selectors that actually win through specificity.
- Next: keep F2 `passes: false` until independent reviewer approval; browser screenshot review remains in the later route/browser tasks.

## 2026-07-11 F2 repository-wide typography remediation
- Changed: normalized 428 font-size rules to complete canonical tuples and replaced curated typography checks with a repository-wide invariant; test and audit selector-list parsing now match.
- Verified: focused contract tests pass (14/14), static audit passes for 32 routes with zero violations and zero important declarations, typecheck passes, and production build passes with 27/27 static pages generated.
- Learned: semantic role validation must run before accepting the current token, especially for 14px interactive controls and table headers; 11px alone is not enough to infer HUD semantics.
- Next: keep F2 `passes: false` until independent reviewer approval; browser screenshot review remains in later tasks.

## 2026-07-11 F2 per-selector semantic remediation
- Changed: classify typography per selector, reject mixed-role selector lists, add explicit ambiguous-class overrides, and split command-primary, native th/td, brand/status, and report-table header typography.
- Verified: focused contract tests pass (16/16), static audit passes for 32 routes with zero violations and zero important declarations, typecheck passes, and production build passes with 27/27 static pages generated.
- Learned: selector-list keywords can leak HUD or table semantics across unrelated selectors; role inference and tuple validation must operate on each selector independently.
- Next: keep F2 `passes: false` until independent reviewer approval; browser screenshot review remains in later tasks.

## 2026-07-11 F2 base module action remediation
- Changed: base and document-variant module primary/action selectors now have explicit control precedence, and the base CSS group uses the full control tuple.
- Verified: focused contract tests pass (18/18), static audit passes for 32 routes with zero violations and zero important declarations, typecheck passes, and production build passes with 27/27 static pages generated.
- Learned: explicit DOM-backed semantics must precede the current font-size token for class-only action selectors.
- Next: keep F2 `passes: false` until independent reviewer approval; browser screenshot review remains in later tasks.

## 2026-07-11 F3 review closure
- Changed: added semantic framework headings, named loading state, stable shell/landing hooks, canonical landing/document cascade values, and reduced-motion spinner handling.
- Verified: focused shared plus design-contract tests 32/32, static audit, typecheck, build 27/27, independent spec and code-quality review pass with zero findings.
- Learned: shared hooks do not prove effective styling when later scoped selectors win; tests must assert the higher-specificity current selectors explicitly.
- Next: F4 all route families.
