# Supporting Evidence

## Fallback interpolation

- Path: $(@{id=evidence-1; label=Fallback interpolation; path=lib/weather.ts; startLine=489; endLine=500; language=typescript; role=source; code=const message = error instanceof Error ? error.message : String(error); detail: `... ${message}`; explanation=Raw upstream error text enters a normal response.}.path):489`n- Role: $(@{id=evidence-1; label=Fallback interpolation; path=lib/weather.ts; startLine=489; endLine=500; language=typescript; role=source; code=const message = error instanceof Error ? error.message : String(error); detail: `... ${message}`; explanation=Raw upstream error text enters a normal response.}.role)`n
`$(@{id=evidence-1; label=Fallback interpolation; path=lib/weather.ts; startLine=489; endLine=500; language=typescript; role=source; code=const message = error instanceof Error ? error.message : String(error); detail: `... ${message}`; explanation=Raw upstream error text enters a normal response.}.language)
const message = error instanceof Error ? error.message : String(error); detail: `... ${message}`
``n
Raw upstream error text enters a normal response.
