# Query logs table is exposed without row-level security

| Field | Value |
| --- | --- |
| Severity | low |
| Confidence | high |
| Category | Missing authorization / RLS |
| CWE | CWE-862 |
| Affected lines | supabase/migrations/001_init.sql:1-6 |

## Summary

query_logs is created without row-level security, exposing stored query text to direct public-schema access under Supabase default grants. Browser Supabase configuration proves intended direct REST capability and no canonical migration adds query_logs RLS; live table grants/existence remain unverified and were not changed.

## Root Cause

The violated invariant is that query logs table is exposed without row-level security. Current source instead shows: The migration creates query_logs with query_text but never enables RLS or defines an authorization policy; later migrations do not retrofit this table.

### Root Control evidence

`supabase/migrations/001_init.sql:1-6`

```sql
create table if not exists query_logs (
  id bigint generated always as identity primary key,
  query_text text not null,
  mode text not null default 'mock',
  created_at timestamptz not null default now()
);
```

This root_control location carries or governs the validated path: The migration creates query_logs with query_text but never enables RLS or defines an authorization policy; later migrations do not retrofit this table.

## Validation

Method: Cumulative canonical migration and browser-client exposure trace

query_logs stores user query text; migration 001 never enables RLS and no later migration or grant revocation closes direct access.

Limitations:
- No application route is needed for direct public-schema access.
- Live grants/table existence are not verified because live RLS evidence is approval-gated.

## Dataflow

Direct Supabase PostgREST anon/authenticated request -> public.query_logs created without RLS/policy/grant revocation -> select/insert/update/delete -> stored query-text disclosure or mutation.

## Reachability

Browser Supabase configuration proves intended direct REST capability and no canonical migration adds query_logs RLS; live table grants/existence remain unverified and were not changed.

Counterevidence: No application route is needed and service-role controls do not protect direct REST; missing live proof limits deployment confidence rather than defeating the canonical policy gap.

## Severity

**low**. The matrix maps medium impact and unknown likelihood to low because query text may be sensitive but live grants and table deployment are not verified.

Change conditions: Raise if live direct access and sensitive query contents are confirmed; ignore if verified live grants revoke access or a later applied policy protects the table.

## Remediation

Enable RLS, restrict writes to a controlled server path, restrict reads to authorized operators, and remove broad public-schema grants.

Regression tests:
- Anonymous and ordinary authenticated roles cannot read query text.
- Only the intended server or operator role can insert and inspect logs.
