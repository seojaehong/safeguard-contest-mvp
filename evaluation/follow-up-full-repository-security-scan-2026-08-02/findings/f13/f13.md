# Share sessions can bind another tenant's site or workpack

| Field | Value |
| --- | --- |
| Severity | low |
| Confidence | high |
| Category | Authorization bypass / share integrity |
| CWE | CWE-639 |
| Affected lines | supabase/migrations/010_commercial_operations.sql:21-34, supabase/migrations/010_commercial_operations.sql:161-176 |

## Summary

workpack_share_sessions policy does not prove that site_id and workpack_id belong to organization_id. Browser Supabase clients and canonical owner policies support direct authenticated REST as a distinct path from service-role application access; exploitation requires a valid account and foreign UUID. Live policy/application state is unverified without approval.

## Root Cause

The violated invariant is that share sessions can bind another tenant's site or workpack. Current source instead shows: The RLS predicate authorizes solely through organization_id, permitting an owned row to reference another tenant's workpack or site if its UUID is known.

### Source evidence

`supabase/migrations/010_commercial_operations.sql:21-34`

```sql
create table if not exists workpack_share_sessions (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references organizations(id) on delete cascade,
  site_id uuid references sites(id) on delete set null,
  workpack_id uuid not null references workpacks(id) on delete cascade,
  share_scope text not null default 'invited' check (share_scope in ('invited','organization')),
  recipients_snapshot jsonb not null default '[]'::jsonb,
  access_policy jsonb not null default '{"anonymousAllowed":false,"manualLanguageSwitchAllowed":true,"requireKnownWorkerSnapshot":true}'::jsonb,
  status text not null default 'active' check (status in ('active','revoked','expired')),
  expires_at timestamptz,
  created_by uuid,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

This source location carries or governs the validated path: The RLS predicate authorizes solely through organization_id, permitting an owned row to reference another tenant's workpack or site if its UUID is known.
### Root Control evidence

`supabase/migrations/010_commercial_operations.sql:161-176`

```sql
create policy "owners can manage workpack share sessions"
  on workpack_share_sessions for all
  using (
    exists (
      select 1 from organizations
      where organizations.id = workpack_share_sessions.organization_id
        and organizations.owner_id = auth.uid()
    )
  )
  with check (
    exists (
      select 1 from organizations
      where organizations.id = workpack_share_sessions.organization_id
        and organizations.owner_id = auth.uid()
    )
  );
```

This root_control location carries or governs the validated path: The RLS predicate authorizes solely through organization_id, permitting an owned row to reference another tenant's workpack or site if its UUID is known.

## Validation

Method: Cumulative canonical migration review and static direct-Supabase RLS proof

The current canonical schema ends at migration 010; site_id and workpack_id are not tenant-bound by policy. No later migration, composite FK, trigger, or policy closes it.

Limitations:
- Application service-role routes often add ownership checks, but those do not protect direct Supabase table access.
- Live pg_policy/catalog and tenant A/B proof are approval-gated and absent; deployment/apply state remains unverified.

## Dataflow

Authenticated direct Supabase PostgREST write supplies an owned organization_id plus a known foreign site_id/workpack_id UUID -> workpack_share_sessions owner policy checks organization_id only -> ordinary FK existence check accepts a cross-tenant relationship -> saved-share relationship integrity. This does not establish exact saved /share reachability: saved Share remains MISSING_EVIDENCE and storage activation is approval-gated.

## Reachability

Browser Supabase clients and canonical owner policies support direct authenticated REST as a distinct path from service-role application access; exploitation requires a valid account and foreign UUID. Live policy/application state is unverified without approval.

Counterevidence: Service-role application routes often perform stronger ownership checks, but those checks do not govern direct table REST. Foreign-table RLS also prevents claiming direct disclosure of the referenced row, so the proven effect is constrained relationship/provenance integrity.

## Severity

**low**. The matrix maps medium impact and medium likelihood to low because cross-tenant integrity is real in canonical policy but requires authentication, identifier knowledge, and deployed grants/migrations.

Change conditions: Raise if direct REST is live and forged relationships are shown to disclose foreign records or drive privileged export/share/mutation; ignore if verified live constraints or policies enforce same-tenant references.

## Remediation

Enforce that site_id and workpack_id belong to organization_id in RLS and database constraints before enabling DB-backed public share creation.

Regression tests:
- Tenant A cannot create or update a session with tenant B workpack or site IDs.
- Exact saved-session geometry remains a separate post-storage verification.
