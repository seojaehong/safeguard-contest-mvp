# Knowledge regeneration runs can forge cross-tenant relationships

| Field | Value |
| --- | --- |
| Severity | low |
| Confidence | high |
| Category | Authorization bypass / provenance integrity |
| CWE | CWE-639 |
| Affected lines | supabase/migrations/003_knowledge_runtime.sql:46-64, supabase/migrations/003_knowledge_runtime.sql:111-126 |

## Summary

knowledge_regeneration_runs RLS allows cross-tenant related-object binding. Browser Supabase clients and canonical owner policies support direct authenticated REST as a distinct path from service-role application access; exploitation requires a valid account and foreign UUID. Live policy/application state is unverified without approval.

## Root Cause

The violated invariant is that knowledge regeneration runs can forge cross-tenant relationships. Current source instead shows: The owner-for-all policy checks only organization_id although site_id, workpack_id, daily_entry_id, and raw_event_ids can identify records outside that organization.

### Source evidence

`supabase/migrations/003_knowledge_runtime.sql:46-63`

```sql
create table if not exists knowledge_regeneration_runs (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references organizations(id) on delete cascade,
  site_id uuid references sites(id) on delete set null,
  workpack_id uuid references workpacks(id) on delete set null,
  daily_entry_id uuid references daily_entries(id) on delete set null,
  question text not null,
  raw_event_ids uuid[] not null default '{}'::uuid[],
  matched_hazards jsonb not null default '[]'::jsonb,
  templates jsonb not null default '[]'::jsonb,
  ai_instruction text not null default '',
  generated_output jsonb not null default '{}'::jsonb,
  provider text,
  status text not null default 'draft' check (
    status in ('draft', 'generated', 'review_required', 'approved', 'failed')
  ),
  created_by uuid,
  created_at timestamptz not null default now()
```

This source location carries or governs the validated path: The owner-for-all policy checks only organization_id although site_id, workpack_id, daily_entry_id, and raw_event_ids can identify records outside that organization.
### Root Control evidence

`supabase/migrations/003_knowledge_runtime.sql:111-126`

```sql
create policy "owners can manage knowledge regeneration runs"
  on knowledge_regeneration_runs for all
  using (
    exists (
      select 1 from organizations
      where organizations.id = knowledge_regeneration_runs.organization_id
        and organizations.owner_id = auth.uid()
    )
  )
  with check (
    exists (
      select 1 from organizations
      where organizations.id = knowledge_regeneration_runs.organization_id
        and organizations.owner_id = auth.uid()
    )
  );
```

This root_control location carries or governs the validated path: The owner-for-all policy checks only organization_id although site_id, workpack_id, daily_entry_id, and raw_event_ids can identify records outside that organization.

## Validation

Method: Cumulative canonical migration review and static direct-Supabase RLS proof

The current canonical schema ends at migration 010; site_id, workpack_id, daily_entry_id, and raw_event_ids are not tenant-bound by policy. No later migration, composite FK, trigger, or policy closes it.

Limitations:
- Application service-role routes often add ownership checks, but those do not protect direct Supabase table access.
- Live pg_policy/catalog and tenant A/B proof are approval-gated and absent; deployment/apply state remains unverified.

## Dataflow

Authenticated direct Supabase PostgREST write supplies an owned organization_id plus a known foreign site_id/workpack_id/daily_entry_id/raw_event_ids UUID -> knowledge_regeneration_runs owner policy checks organization_id only -> ordinary FK existence check accepts a cross-tenant relationship -> regeneration provenance integrity.

## Reachability

Browser Supabase clients and canonical owner policies support direct authenticated REST as a distinct path from service-role application access; exploitation requires a valid account and foreign UUID. Live policy/application state is unverified without approval.

Counterevidence: Service-role application routes often perform stronger ownership checks, but those checks do not govern direct table REST. Foreign-table RLS also prevents claiming direct disclosure of the referenced row, so the proven effect is constrained relationship/provenance integrity.

## Severity

**low**. The matrix maps medium impact and medium likelihood to low because cross-tenant integrity is real in canonical policy but requires authentication, identifier knowledge, and deployed grants/migrations.

Change conditions: Raise if direct REST is live and forged relationships are shown to disclose foreign records or drive privileged export/share/mutation; ignore if verified live constraints or policies enforce same-tenant references.

## Remediation

Check that every related site, workpack, and daily entry belongs to organization_id before insert or update and enforce the tuple at the database layer.

Regression tests:
- Reject regeneration runs with any foreign-tenant relationship.
- Verify same-tenant regeneration history remains queryable.
