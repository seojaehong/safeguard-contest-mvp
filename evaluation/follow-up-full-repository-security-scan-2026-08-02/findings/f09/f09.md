# Workpack improvements can bind cross-tenant workpacks or sites

| Field | Value |
| --- | --- |
| Severity | low |
| Confidence | high |
| Category | Authorization bypass / tenant integrity |
| CWE | CWE-639 |
| Affected lines | supabase/migrations/010_commercial_operations.sql:51-69, supabase/migrations/010_commercial_operations.sql:195-210 |

## Summary

workpack_improvements policy permits cross-tenant workpack and site associations. Browser Supabase clients and canonical owner policies support direct authenticated REST as a distinct path from service-role application access; exploitation requires a valid account and foreign UUID. Live policy/application state is unverified without approval.

## Root Cause

The violated invariant is that workpack improvements can bind cross-tenant workpacks or sites. Current source instead shows: The owner policy checks organization_id only and leaves workpack_id and site_id consistency unenforced, allowing integrity-impacting improvement rows on known foreign identifiers.

### Source evidence

`supabase/migrations/010_commercial_operations.sql:51-68`

```sql
create table if not exists workpack_improvements (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references organizations(id) on delete cascade,
  site_id uuid references sites(id) on delete set null,
  workpack_id uuid not null references workpacks(id) on delete cascade,
  task_label text not null,
  hazard_label text not null,
  improvement_text text not null,
  reflected_documents text[] not null default '{}'::text[],
  review_status text not null default 'candidate' check (review_status in ('candidate','approved','rejected','reflected')),
  source_type text not null default 'manual' check (source_type in ('manual','photo_analysis','operator_note')),
  photo_summary jsonb not null default '{}'::jsonb,
  analysis_payload jsonb not null default '{}'::jsonb,
  created_by uuid,
  approved_by uuid,
  approved_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
```

This source location carries or governs the validated path: The owner policy checks organization_id only and leaves workpack_id and site_id consistency unenforced, allowing integrity-impacting improvement rows on known foreign identifiers.
### Root Control evidence

`supabase/migrations/010_commercial_operations.sql:195-210`

```sql
create policy "owners can manage workpack improvements"
  on workpack_improvements for all
  using (
    exists (
      select 1 from organizations
      where organizations.id = workpack_improvements.organization_id
        and organizations.owner_id = auth.uid()
    )
  )
  with check (
    exists (
      select 1 from organizations
      where organizations.id = workpack_improvements.organization_id
        and organizations.owner_id = auth.uid()
    )
  );
```

This root_control location carries or governs the validated path: The owner policy checks organization_id only and leaves workpack_id and site_id consistency unenforced, allowing integrity-impacting improvement rows on known foreign identifiers.

## Validation

Method: Cumulative canonical migration review and static direct-Supabase RLS proof

The current canonical schema ends at migration 010; workpack_id and site_id are not tenant-bound by policy. No later migration, composite FK, trigger, or policy closes it.

Limitations:
- Application service-role routes often add ownership checks, but those do not protect direct Supabase table access.
- Live pg_policy/catalog and tenant A/B proof are approval-gated and absent; deployment/apply state remains unverified.

## Dataflow

Authenticated direct Supabase PostgREST write supplies an owned organization_id plus a known foreign workpack_id/site_id UUID -> workpack_improvements owner policy checks organization_id only -> ordinary FK existence check accepts a cross-tenant relationship -> improvement relationship integrity.

## Reachability

Browser Supabase clients and canonical owner policies support direct authenticated REST as a distinct path from service-role application access; exploitation requires a valid account and foreign UUID. Live policy/application state is unverified without approval.

Counterevidence: Service-role application routes often perform stronger ownership checks, but those checks do not govern direct table REST. Foreign-table RLS also prevents claiming direct disclosure of the referenced row, so the proven effect is constrained relationship/provenance integrity.

## Severity

**low**. The matrix maps medium impact and medium likelihood to low because cross-tenant integrity is real in canonical policy but requires authentication, identifier knowledge, and deployed grants/migrations.

Change conditions: Raise if direct REST is live and forged relationships are shown to disclose foreign records or drive privileged export/share/mutation; ignore if verified live constraints or policies enforce same-tenant references.

## Remediation

Require workpack_id and site_id to belong to organization_id and to each other through RLS plus database constraints or triggers.

Regression tests:
- Reject cross-tenant workpack and site IDs on insert and update.
- Accept only same-tenant, internally consistent improvement rows.
